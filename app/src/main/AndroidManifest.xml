<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.houlijiang.common"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/CommonAppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.TestNetActivity" />
        <activity android:name=".ui.activity.TestListViewActivity" />
        <activity android:name=".ui.activity.TestImageLoaderActivity" />
        <activity android:name=".ui.activity.TestMp3RecActivity" />
        <activity android:name=".ui.activity.TestCacheActivity" />
        <activity android:name=".ui.activity.TestImageBrowserActivity" />

        <activity android:name=".ui.activity.LoginActivity" />
    </application>

</manifest>
